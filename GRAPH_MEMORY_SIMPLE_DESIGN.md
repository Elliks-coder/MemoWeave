# Graph 记忆链路：简版设计说明

> 文档状态：方案评审稿，尚未实现。

## 1. 想解决什么问题

当前向量检索擅长找到“和问题语义相似的一条记忆”，但不擅长把分散在不同对话中的多条事实连接起来。

例如用户先后说过：

- 对话 A：`小林是我的同事。`
- 对话 B：`小林最近从上海搬到了杭州。`

当问题是“用户哪位同事搬去了杭州？”时，需要组合两条记忆：

```text
用户 --同事--> 小林 --现居--> 杭州
```

Graph 记忆的目标就是补齐这类跨对话、多跳关系。它是向量检索的补充，不替代 Chroma，也不直接替 Agent 编造答案。

## 2. 从哪里建图

采用 **“L1 提取候选，L2 审核发布”**，而不是只选 L1 或只选 L2。

```text
L1 原始对话
   │  抽取实体和候选关系
   ▼
L2 reconcile：去重、消歧、处理时间与冲突
   │  只有审核通过的事实才能发布
   ▼
Active Graph：供检索使用的可信关系
```

- **为什么读取 L1：**原始对话保留了完整的人名、事件和上下文，能够减少信息遗漏。
- **为什么不能直接信任 L1：**一句话可能是否定、误述、旧状态或指代不清，直接建永久边容易产生脏关系。
- **为什么由 L2 发布：**L2 已完成事实归一、冲突消解、时间标注和版本演化，适合作为当前有效事实的权威来源。

因此，L1 是证据源，L2 是事实源，Graph 是关系索引。

## 3. 图里存什么

第一版只设计五种节点：

| 节点 | 作用 | 示例 |
|---|---|---|
| `Entity` | 人、地点、组织、物品等实体 | 小林、杭州、某公司 |
| `Assertion` | 一条带时间和状态的关系声明 | 小林现居杭州 |
| `FactRef` | 指向 Chroma 中的 L2 事实 | `l2_id=...` |
| `EpisodeRef` | 指向原始 session / turn | `dia_id=...` |
| `Schema` | 已有的 L6 高阶行为模式 | 用户偏好规律 |

逻辑上，一条关系看起来是：

```text
小林 --LIVES_IN--> 杭州
```

实际存储时使用 `Assertion` 节点承载关系属性：

```text
(小林) <-SUBJECT- (Assertion) -OBJECT-> (杭州)
                         │
                    SUPPORTED_BY
                         ▼
                      (FactRef) -FROM_EPISODE-> (EpisodeRef)
```

`Assertion` 保存：

- `predicate`：`COLLEAGUE_OF`、`LIVES_IN`、`WORKS_AT`、`LIKES` 等；
- `status`：`candidate / active / superseded / conflicted / rejected`；
- `confidence`；
- `observed_at`、`valid_from`、`valid_until`；
- `user_id / agent_id`，用于租户隔离。

关系单独建成节点，是因为同一个关系需要记录来源、置信度、有效时间和版本，普通的一条边不便于完整表达这些信息。

L6 仍通过 `DERIVED_FROM` 指向支持它的 `FactRef`，不与实体事实图混为一层。

## 4. 写入和更新流程

1. 保存 L1 原始对话，并保留 session / turn ID。
2. 从 L1 抽取实体和候选关系，但候选关系暂不参与检索。
3. 经过现有 reconcile 生成或更新 L2。
4. 只有具备有效 L2、明确 provenance 和可解析主体/客体的关系，才转为 `active`。
5. 如果用户后来表达了新状态，旧关系标记为 `superseded`，保留时间演化链，不直接删除。

例如：

```text
旧事实：小林居住在上海（valid_until=2026-05）
新事实：小林居住在杭州（valid_from=2026-05）
新 Assertion --SUPERSEDES--> 旧 Assertion
```

对于“喜欢摄影”这类可以并存的关系，不因新增关系而覆盖旧关系；只有“当前居住地”“当前公司”等单值状态才进行替代。

## 5. 查询时怎么使用

第一版不引入复杂路由，所有查询都执行一次受限的图补充：

1. Chroma 先召回 L2/L7，得到语义相关的事实和实体种子。
2. Graph 从种子实体出发，最多扩展 2 跳。
3. 只保留能够指回有效 L2 的关系路径。
4. 根据 `FactRef` 回到 Chroma 读取事实原文。
5. 将向量结果和图补充事实合并、去重、重排后交给回答模型。

以上面的提问为例，Graph 找到：

```text
用户 --COLLEAGUE_OF--> 小林 --LIVES_IN--> 杭州
```

但提交给回答模型的不是裸图，而是两条可核验的 L2 事实：

- `小林是用户的同事。`（来源：对话 A / turn x）
- `小林已搬到杭州。`（来源：对话 B / turn y）

这样既利用了图的连接能力，又保留了原始证据和可解释性。

## 6. 如何防止图带来噪声

第一版使用以下硬规则：

- 只查询当前 `user_id / agent_id` 下的数据；
- 只有 `active` 且存在 L2 provenance 的关系可以进入回答上下文；
- 有时间的问题按 `valid_from / valid_until` 过滤历史状态；
- 实体合并采取保守策略：宁可暂时不合并，也不把两个同名人物误合并；
- “两个人在同一段对话出现”只算弱共现，不能直接当作朋友、同事等事实关系；
- 查询最多 2 跳，限制高出度节点和补充事实数量，避免图无限扩散；
- 图路径只负责找证据，回答仍必须由 L2/L1 证据支持。

## 7. 怎么验证它是否真的有效

采用同一批问题做严格 A/B 测试，模型、提示词、向量 Top-K 和阈值保持不变：

- **A 组：**现有向量检索；
- **B 组：**现有向量检索 + Graph 受限扩展。

重点观察四类指标：

| 维度 | 指标 | 目的 |
|---|---|---|
| 建图质量 | 关系准确率、实体误合并率、provenance 完整率 | 确认图本身可信 |
| 检索效果 | Multi-hop Recall@10、完整证据召回率 | 确认图找到了缺失证据 |
| 回答效果 | Multi-hop Accuracy | 确认新增证据真正改善答案 |
| 副作用 | Single-hop / Adversarial Accuracy、P95 延迟 | 防止噪声、幻觉和性能退化 |

当前 500 题基线中：

- Multi-hop Recall@10：**45.74%**；
- Multi-hop Accuracy：**41.00%**；
- Single-hop Accuracy：**75.00%**；
- Adversarial Accuracy：**90.62%**。

第一版建议验收标准：

- 人工抽检的显式关系准确率不低于 **90%**；
- provenance 完整率达到 **100%**，实体误合并率低于 **1%**；
- Multi-hop Recall@10 至少提升 **8 个百分点**；
- Multi-hop Accuracy 至少提升 **5 个百分点**；
- Single-hop Accuracy 下降不超过 **2 个百分点**，对抗题不明显下降；
- 检索 P95 延迟增幅不超过 **30%**。

每道题还要记录“向量原结果、图新增事实、采用的路径、答案是否变化”，把图的影响分成：有效提升、无影响、有害干扰。不能只看总分。

## 8. 第一版明确不做什么

- 不直接把 L1 抽取结果写成可信的永久关系；
- 不把完整 L2 文本复制到图数据库；
- 不做全图社区摘要、PageRank 或复杂问题路由；
- 不自动把所有邻居都塞给回答模型；
- 不修改现有 L6 的职责，也不让 L6 代替实体事实图。

## 9. 推荐落地顺序

1. **Shadow 模式：**只建图和记录结果，不影响当前回答。
2. **离线核验：**人工抽检关系、实体合并和 provenance。
3. **A/B 评测：**在开发集调通，再用新的未观察题集做最终比较。
4. **受控上线：**达成验收标准后，才让图补充事实进入回答上下文。

一句话总结：**L1 保证不漏，L2 保证不乱，Graph 负责把跨对话事实连起来，最终答案仍由可追溯证据负责。**
